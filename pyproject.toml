[project]
name = "ws-calendar"
version = "0.1.0"
description = "Generate macro calendar ICS files"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.0",
    "requests>=2.32.0",
]

[tool.uv]
package = false
